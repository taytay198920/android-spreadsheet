# 🚨 紧急修复：编译错误

## 问题
1. `Color` 类未找到 - 需要导入 `material.dart`
2. `ColorPicker` 未找到 - `flutter_colorpicker` 包可能有问题

## 立即修复方案

### 方案A：使用简单颜色选择器替代
```dart
// 在cell_widget.dart中替换ColorPicker
_showColorPicker(BuildContext context, int row, int col) {
  showDialog(
    context: context,
    builder: (context) {
      return AlertDialog(
        title: Text('选择颜色'),
        content: Column(
          mainAxisSize: MainAxisSize.min,
          children: [
            _buildColorOption(context, row, col, Colors.red, '红色'),
            _buildColorOption(context, row, col, Colors.green, '绿色'),
            _buildColorOption(context, row, col, Colors.blue, '蓝色'),
            _buildColorOption(context, row, col, Colors.yellow, '黄色'),
            _buildColorOption(context, row, col, Colors.white, '白色'),
          ],
        ),
      );
    },
  );
}
```

### 方案B：移除颜色选择器功能（立即构建）
暂时注释掉颜色选择相关代码，先确保APK能构建成功。

## 执行步骤
运行以下修复脚本：